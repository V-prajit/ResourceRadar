import React from "react";

const SystemGraph = ( {timeFrame} ) => {



    return (
        <div>
            <p> Let us see if this is working {timeFrame}</p>
        </div>
    );
};

export default SystemGraph;